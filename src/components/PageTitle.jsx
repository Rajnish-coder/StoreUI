function PageTitle({ title }) {
  return (
    <>
      <h1 className="text-lg font-primary font-extrabold text-center text-primary dark:text-light py-2">
        {title}
      </h1>
    </>
  );
}

export default PageTitle;
